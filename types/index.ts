
export enum statusRender {
    edit = 'EDIT',
    normal = 'NORMAL',
    delete = 'DELETE'
}
export type dropdown = {
    label: string
    value: number
}

export enum tabEnum {
    user,
    app,
    ownership
}